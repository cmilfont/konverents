<h2><%= @event.title %></h2>

<%= form_for @track, :url => { :action => "create" } do |f| %>

  <%= f.hidden_field :event_id, :value => @track.event_id %>

  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :date %>
  <%= f.text_field :date %>

  <%= f.submit "Create" %>
<% end %>
